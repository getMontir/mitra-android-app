<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="20dp"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    tools:viewBindingIgnore="true">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:src="@drawable/ic_walkthrough_01"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/slideTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/walkthrough_first_title"
        android:textAlignment="center"
        android:gravity="center"
        style="@style/TextAppearance.GetMontirMitra.Headline6"
        android:textStyle="bold"
        android:textColor="@color/secondaryColor"
        android:layout_marginTop="20dp"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/slideContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/walkthrough_first_description"
        android:textAlignment="center"
        android:gravity="center"
        style="@style/TextAppearance.GetMontirMitra.Subtitle2"
        android:textColor="@color/black_blue_900"
        android:layout_marginTop="10dp"/>

</LinearLayout>